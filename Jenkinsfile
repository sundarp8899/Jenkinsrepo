// this is main file

pipeline {
    agent {
        label 'slave-java'
    }
    stages {
        stage ('build stage') {
            steps {
                echo "this is from label and scm"
            }
        }
    }
}
